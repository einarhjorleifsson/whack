% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/positions.R
\name{wk_convert_ddmmss}
\alias{wk_convert_ddmmss}
\title{Convert signed ddmmss integer encoding to decimal degrees}
\usage{
wk_convert_ddmmss(x, validate = TRUE)
}
\arguments{
\item{x}{A numeric/integer vector encoded as signed \code{ddmmss}.}

\item{validate}{Boolean (default TRUE).}
}
\value{
A numeric vector of decimal degrees (type \code{double}), with \code{NA_real_}
where \code{x} is \code{NA}.
}
\description{
Converts values encoded as \code{ddmmss} (degrees-minutes-seconds) into decimal
degrees.
}
\details{
The input is typically an integer with up to 6 digits where:
\itemize{
\item \code{dd} is (absolute) degrees (0–180)
\item \code{mm} is minutes (0–59)
\item \code{ss} is seconds (0–59)
}

The sign of \code{x} is preserved in the output. Values with fewer than 6 digits
are interpreted as right-aligned \code{ddmmss} components (e.g. \code{30} is treated as
30 seconds, \code{5959} as 59 minutes 59 seconds, \code{10000} as 1 degree).

When \code{validate = TRUE}, the function checks that minutes and seconds are in
\verb{[0, 59]} and that absolute degrees are \verb{<= 180}; otherwise it throws an error.
}
\examples{
x <- c(-30, -5959, -65559, -173000)
wk_convert_ddmmss(x)
# -0.008333333  -0.999722222  -6.933055556 -17.500000000

# Validation: minutes/seconds must be in 0..59
\dontrun{
wk_convert_ddmmss(c(126199))  # 61 minutes -> error
}

}
