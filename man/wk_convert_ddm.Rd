% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/positions.R
\name{wk_convert_ddm}
\alias{wk_convert_ddm}
\title{Convert degrees-decimal minutes to decimal degrees}
\usage{
wk_convert_ddm(x, validate = TRUE)
}
\arguments{
\item{x}{Numeric vector encoded as \code{ddmmdm}.}

\item{validate}{Logical (default \code{TRUE}). If \code{TRUE}, validates that minutes
are in \verb{[0, 60)} and degrees are \verb{<= 180}.}
}
\value{
A numeric vector of decimal degrees (type \code{double}), with \code{NA_real_}
where \code{x} is \code{NA}.
}
\description{
Converts values encoded as \code{ddmmdm} (degrees and decimal minutes) into
decimal degrees.
}
\details{
The encoding is \code{dd * 10000 + decimal_minutes * 100}, where:
\itemize{
\item \code{dd} is (absolute) degrees (0–180)
\item \code{decimal_minutes} is minutes expressed as a decimal number (0–59.999...)
}

For example, \code{643050} encodes 64 degrees and 30.50 minutes
(64 * 10000 + 30.50 * 100). Non-integer inputs are supported: the fractional
part of \code{x} extends precision beyond hundredths of a minute. For instance,
\code{643050.99} encodes 64 degrees and 30.5099 minutes.

The sign of \code{x} is preserved in the output.

When \code{validate = TRUE}, the function checks that the minutes component
(i.e. \code{x \%\% 10000}) is less than \code{6000} (i.e. < 60 minutes) and that
absolute degrees are \verb{<= 180}; otherwise it throws an error.
}
\examples{
# 64°30.50' N and 22°45.00' W (integer input)
wk_convert_ddm(c(643050, -224500))
# 64.50833  -22.75000

# Decimal input: 643050.99 encodes 64°30.5099' (sub-hundredth precision)
wk_convert_ddm(643050.99)
# 64.50850

# Validation: minutes must be < 60
\dontrun{
wk_convert_ddm(6000)  # 60 minutes -> error
}

}
\seealso{
\code{\link[geo:geoconvert.1]{geo::geoconvert.1()}}, from which the conversion formula is
directly adapted.
}
