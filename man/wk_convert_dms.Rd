% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/positions.R
\name{wk_convert_dms}
\alias{wk_convert_dms}
\title{Convert degrees-minutes-seconds to decimal degrees}
\usage{
wk_convert_dms(x, validate = TRUE)
}
\arguments{
\item{x}{Numeric vector encoded as \code{ddmmss}.}

\item{validate}{Logical (default \code{TRUE}). If \code{TRUE}, validates that minutes
are in \verb{[0, 59]}, seconds are in \verb{[0, 60)}, and degrees are \verb{<= 180}.}
}
\value{
A numeric vector of decimal degrees (type \code{double}), with \code{NA_real_}
where \code{x} is \code{NA}.
}
\description{
Converts values encoded as \code{ddmmss} (degrees-minutes-seconds) into decimal
degrees.
}
\details{
The encoding is \code{dd * 10000 + mm * 100 + ss}, where:
\itemize{
\item \code{dd} is (absolute) degrees (0–180)
\item \code{mm} is whole minutes (00–59)
\item \code{ss} is seconds (0–59.999...)
}

For example, \code{65595930} encodes 65°59'59.30". Non-integer inputs are
supported: the fractional part of \code{x} extends the seconds beyond whole
numbers. For instance, \code{655959.5} encodes 65°59'59.5".

The sign of \code{x} is preserved in the output. Values with fewer than 6 digits
are interpreted as right-aligned \code{ddmmss} components (e.g. \code{30} is treated
as 30 seconds, \code{5959} as 59 minutes 59 seconds, \code{10000} as 1 degree).

When \code{validate = TRUE}, the function checks that minutes are in \verb{[0, 59]},
seconds are in \verb{[0, 60)}, and absolute degrees are \verb{<= 180}; otherwise it
throws an error.
}
\examples{
x <- c(-30, -5959, -65559, -173000)
wk_convert_dms(x)
# -0.008333333  -0.999722222  -6.933055556 -17.500000000

# Validation: minutes/seconds must be in [0, 59]
\dontrun{
wk_convert_dms(c(126199))  # 61 minutes -> error
}

}
